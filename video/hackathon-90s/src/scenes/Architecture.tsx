import { Beat } from "../Beat";
export const Architecture = () => (
  <Beat
    name="Architecture"
    title="Decisions shape what happens next."
    subtitle="Multiple paths. Evidence behind every decision."
    asset="handoff"
    label="GPT-6 Astra + Agents API"
  />
);
