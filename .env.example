# Server-only OpenAI project API key. Required for both real provider probes.
OPENAI_API_KEY=

# Optional overrides; blank values use the documented defaults.
PORT=
APP_ORIGIN=
OPENAI_LIVE_MODEL=
OPENAI_EXAMINER_MODEL=
